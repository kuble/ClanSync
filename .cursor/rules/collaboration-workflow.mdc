---
description: 의미별 커밋·작업 브랜치 자동 푸시 — 별도 수동 배포·main 병합은 사용자 요청 범위에 따라 수행
alwaysApply: true
---

# 협업 워크플로 (ClanSync)

사용자는 코드를 최대한 직접 수정하지 않고, 에이전트 중심(바이브 코딩)으로 진행한다.

## 커밋·푸시·브랜치 (에이전트 전담)

- **`git commit`**: [git-nano-commit.mdc](./git-nano-commit.mdc)대로 의미 단위로 수행한다.
- **`git push`**: 사용자의 위임(2026-09-15)에 따라 이번 작업 커밋을 현재 작업 브랜치에 자동 푸시한다. 보류 지시가 있으면 따른다. 별도 수동 배포는 명시 요청 시에만 수행한다.
- **브랜치 생성·전환**: 필요 시 작업 브랜치를 만들고 그 위에서 커밋한다. 이름은 적용 중인 환경 규칙과 사용자 지시를 따른다.
- 사용자가 커밋·브랜치 작업 보류를 명시하면 해당 작업을 생략한다.

## `main` 반영 — 사용자 컨펌 (유일한 게이트)

- **다른 브랜치(feature 등)의 변경을 `main`에 합치려 할 때** (로컬 머지, `main`에 직접 푸시, **PR 작성·머지 준비·머지 실행** 직전)에는 **반드시 사용자에게 한 번 확인**받는다.
- 컨펌 전에는: 변경 요약, 브랜치명, PR 초안(제목·핵심만)을 짧게 제시한다.
- **현재 작업 브랜치가 이미 `main`인 경우**에도 나노 커밋과 위임된 자동 푸시를 수행한다. Git 연동에 따른 Vercel 자동 빌드는 해당 푸시에 수반된다.

## 사용자 역할 (기대치)

- 기능·범위·우선순위 설명, 화면·동작 확인, **feature→`main` 반영( PR/머지) 최종 승인만** 사용자가 한다.
- UI·기능 검수용 **절차·시나리오 누적**: `docs/QA_시나리오.md` + [qa-demo-handoff.mdc](./qa-demo-handoff.mdc).
